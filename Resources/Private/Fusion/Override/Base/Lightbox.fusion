prototype(Litespeed.Theme:Document.Lightbox.PhotoSwipe) {
    prototype(Litespeed.Presentation:Atom.Clippath.Component) {
        @if.hasClipPath = ${q(site).property('themeClipPathEnabled') && q(site).property('themeClipPathInContent')}
    }
}
